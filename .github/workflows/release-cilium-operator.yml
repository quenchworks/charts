name: release-cilium-operator

on:
  push:
    branches: [main]
    paths:
      - "quench/cilium-operator/**"
      - ".github/workflows/release-cilium-operator.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write        # push the OCI chart to GHCR
  id-token: write

env:
  CHART_DIR: quench/cilium-operator
  OCI_REPO: oci://ghcr.io/quenchworks/charts

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: azure/setup-helm@v5
      # Cilium is the CNI, so kind starts without its default one
      - name: Write kind config
        run: |
          cat > /tmp/kind-cilium.yaml <<'EOF'
          kind: Cluster
          apiVersion: kind.x-k8s.io/v1alpha4
          networking:
            disableDefaultCNI: true
          nodes:
            - role: control-plane
            - role: worker
          EOF
      - uses: helm/kind-action@v1
        with:
          config: /tmp/kind-cilium.yaml
          wait: 0s
      - uses: sigstore/cosign-installer@v3

      - name: Log in to GHCR (for cosign)
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lint and template
        run: |
          helm dependency build "${CHART_DIR}"
          helm lint "${CHART_DIR}" -f "${CHART_DIR}/ci/default-values.yaml"
          helm template "${CHART_DIR}" -f "${CHART_DIR}/ci/default-values.yaml" >/dev/null

      # Upstream Cilium agents with their own operator OFF: without an operator no
      # CiliumNode gets a pod CIDR, so no node turns Ready. Not waited on for that reason.
      - name: Install Cilium agents (no operator)
        run: |
          helm repo add cilium https://helm.cilium.io
          helm install cilium cilium/cilium --version 1.20.2 -n kube-system \
            --set operator.enabled=false --set hubble.enabled=false

      - name: Install gate (kind)
        run: |
          set -o pipefail
          helm install rtest "${CHART_DIR}" -n kube-system -f "${CHART_DIR}/ci/default-values.yaml" --wait --timeout 6m
          kubectl -n kube-system rollout status deployment/rtest-cilium-operator --timeout=120s
          # this operator alone registers the CRDs (one at a time, after it wins the lease)
          # and hands out the pod CIDRs
          CRDS="ciliumnodes.cilium.io ciliumidentities.cilium.io ciliumendpoints.cilium.io"
          for i in $(seq 1 90); do kubectl get crd $CRDS >/dev/null 2>&1 && break; sleep 2; done
          for c in $CRDS; do kubectl wait --for=condition=Established "crd/$c" --timeout=60s; done
          kubectl -n kube-system rollout status daemonset/cilium --timeout=300s
          kubectl wait --for=condition=Ready nodes --all --timeout=300s
          for n in $(kubectl get nodes -o name | cut -d/ -f2); do
            cidr="$(kubectl get ciliumnode "$n" -o jsonpath='{.spec.ipam.podCIDRs[0]}')"
            echo "$n $cidr"; test -n "$cidr"
          done
          # pods get addresses from those CIDRs and reach each other through Cilium
          kubectl -n kube-system rollout status deployment/coredns --timeout=180s
          kubectl run gate-dns --rm -i --restart=Never --image=ghcr.io/quenchworks/images/busybox:1.37.0 --command -- \
            busybox nslookup kubernetes.default.svc.cluster.local | tee /tmp/dns.txt
          grep -q 'Address' /tmp/dns.txt
          # one leader of two replicas
          kubectl -n kube-system get lease cilium-operator-resource-lock -o jsonpath='{.spec.holderIdentity}' | grep .
          ! kubectl -n kube-system logs deployment/rtest-cilium-operator | grep -E 'level=(fatal|panic)'
      # A failed install gate otherwise reports only "Progress deadline exceeded",
      # which says a pod never became ready and nothing about why: helm --wait
      # aborts before the smoke checks, so no logs are kept. Per container, not
      # --all-containers, because a chatty sidecar spends the whole tail; and a
      # large tail, because a crash floods the log with every open request failing
      # at once and buries the error that triggered it.
      - name: Why the install gate failed
        if: failure()
        run: |
          kubectl get pods -A -o wide || true
          kubectl -n kube-system describe pods -l app.kubernetes.io/instance=rtest || true
          kubectl get events -A --sort-by=.lastTimestamp | tail -40 || true
          for p in $(kubectl -n kube-system get pods -l app.kubernetes.io/instance=rtest -o name); do
            for c in $(kubectl -n kube-system get "$p" -o jsonpath='{.spec.containers[*].name}'); do
              echo "=== $p [$c] current"
              kubectl -n kube-system logs "$p" -c "$c" --tail=400 || true
              echo "=== $p [$c] previous"
              kubectl -n kube-system logs "$p" -c "$c" --previous --tail=400 || true
            done
          done

      - name: Package and push (OCI)
        id: push
        run: |
          set -o pipefail
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin
          helm package "${CHART_DIR}" -d ./dist
          helm push ./dist/cilium-operator-*.tgz "${OCI_REPO}" 2>&1 | tee push.txt
          digest="$(awk '/Digest:/ {print $2}' push.txt)"
          test -n "$digest"
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"

      - name: Sign chart (cosign keyless)
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign "ghcr.io/quenchworks/charts/cilium-operator@${{ steps.push.outputs.digest }}"

      - name: Set up oras
        uses: oras-project/setup-oras@v2

      - name: Publish ArtifactHub metadata (verified publisher)
        run: |
          if grep -qE '^repositoryID:[[:space:]]*REPLACE_WITH' "${CHART_DIR}/artifacthub-repo.yml"; then
            echo "::warning::artifacthub-repo.yml has a placeholder repositoryID; skipping metadata push."
            exit 0
          fi
          echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin
          cd "${CHART_DIR}"
          oras push ghcr.io/quenchworks/charts/cilium-operator:artifacthub.io \
            --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
            "artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml"
