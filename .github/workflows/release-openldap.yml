name: release-openldap

on:
  push:
    branches: [main]
    paths:
      - "quench/openldap/**"
      - ".github/workflows/release-openldap.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write        # push the OCI chart to GHCR
  id-token: write

env:
  CHART_DIR: quench/openldap
  OCI_REPO: oci://ghcr.io/quenchworks/charts

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      # pin actions to SHAs before going public (renovate)
      - uses: actions/checkout@v5
      - uses: azure/setup-helm@v5
      - uses: helm/kind-action@v1
      - uses: sigstore/cosign-installer@v3

      # cosign signs by pushing to GHCR and reads ~/.docker/config.json for creds.
      # docker/login-action writes there (helm/oras logins do not).
      - name: Log in to GHCR (for cosign)
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lint and template
        run: |
          helm dependency build "${CHART_DIR}"
          helm lint "${CHART_DIR}" -f "${CHART_DIR}/ci/default-values.yaml"
          helm template "${CHART_DIR}" -f "${CHART_DIR}/ci/default-values.yaml" >/dev/null

      - name: Install gate (kind)
        run: |
          helm install rtest "${CHART_DIR}" -f "${CHART_DIR}/ci/default-values.yaml" --wait --timeout 5m
          kubectl rollout status statefulset/rtest-openldap --timeout=120s
          pw="$(kubectl get secret rtest-openldap -o jsonpath='{.data.admin-password}' | base64 -d)"
          # real directory checks, not a TCP probe: authenticated bind, then a search
          # that must return the seeded base entry
          kubectl exec rtest-openldap-0 -- \
            ldapwhoami -x -H ldap://127.0.0.1:1389 -D "cn=admin,dc=example,dc=org" -w "$pw" \
            | grep -qi '^dn:cn=admin,dc=example,dc=org$'
          kubectl exec rtest-openldap-0 -- \
            ldapsearch -x -LLL -H ldap://127.0.0.1:1389 -b "dc=example,dc=org" -s base dn \
            | grep -q '^dn: dc=example,dc=org$'
          # and a real write through the admin bind
          kubectl exec -i rtest-openldap-0 -- \
            ldapadd -x -H ldap://127.0.0.1:1389 -D "cn=admin,dc=example,dc=org" -w "$pw" <<'LDIF'
          dn: ou=users,dc=example,dc=org
          objectClass: organizationalUnit
          ou: users
          LDIF
          kubectl exec rtest-openldap-0 -- \
            ldapsearch -x -LLL -H ldap://127.0.0.1:1389 -b "dc=example,dc=org" "(ou=users)" dn \
            | grep -q '^dn: ou=users,dc=example,dc=org$'

      - name: Package and push (OCI)
        id: push
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin
          helm package "${CHART_DIR}" -d ./dist
          helm push ./dist/openldap-*.tgz "${OCI_REPO}" 2>&1 | tee push.txt
          digest="$(awk '/Digest:/ {print $2}' push.txt)"
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"

      - name: Sign chart (cosign keyless)
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign "ghcr.io/quenchworks/charts/openldap@${{ steps.push.outputs.digest }}"

      - name: Set up oras
        uses: oras-project/setup-oras@v2

      - name: Publish ArtifactHub metadata (verified publisher)
        run: |
          if grep -qE '^repositoryID:[[:space:]]*REPLACE_WITH' "${CHART_DIR}/artifacthub-repo.yml"; then
            echo "::warning::artifacthub-repo.yml has a placeholder repositoryID; skipping metadata push. Register the ArtifactHub repo and set the ID."
            exit 0
          fi
          echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin
          cd "${CHART_DIR}"
          oras push ghcr.io/quenchworks/charts/openldap:artifacthub.io \
            --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
            "artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml"
