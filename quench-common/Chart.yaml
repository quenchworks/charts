apiVersion: v2
name: quench-common
description: Quenchworks shared Helm library chart. Labels, hardened security contexts, and a digest-only image resolver.
type: library
version: 0.0.0
appVersion: "0.0.0"
maintainers:
  - name: mkabumattar
home: https://quenchworks.mkabumattar.com
sources:
  - https://github.com/quenchworks/charts
# NOTE: vendored inside the charts repo for now. It will move to its own
# quenchworks/common repo later and be consumed via OCI; app charts will then
# switch their dependency from file://../../quench-common to oci://ghcr.io/quenchworks/charts.
