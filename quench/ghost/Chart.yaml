apiVersion: v2
name: ghost
description: Hardened Ghost publishing platform (Node.js CMS + membership/newsletter engine) on a 0-CVE nonroot image, with a bundled MariaDB backend.
type: application
version: 0.0.7
appVersion: "6.54.0"
kubeVersion: ">=1.23.0-0"
home: https://quench-works.com
sources:
  - https://github.com/quenchworks/charts
  - https://github.com/TryGhost/Ghost
icon: https://raw.githubusercontent.com/quenchworks/.github/main/profile/assets/chart-icon.png
keywords:
  - ghost
  - cms
  - blog
  - publishing
  - newsletter
  - nodejs
maintainers:
  - name: mkabumattar
    email: info@quench-works.com
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "Update Ghost to 6.54.0 (from 6.52.1)."
    - kind: security
      description: "Ghost image rebuilt from source with 0 fixable CVEs (dependency overrides for the tracked advisories)."
  # Ghost is a content-publishing web application; the ArtifactHub category enum
  # (ai-machine-learning, database, integration-delivery, monitoring-logging, networking,
  # security, skip-prediction, storage, streaming-messaging) has no CMS/web-app bucket.
  # "skip-prediction" is AH's explicit "no category fits" value — the honest choice for a
  # CMS (mirrors quench/mattermost, the nearest general-app analog in this catalog).
  category: skip-prediction
  licenses: MIT
  artifacthub.io/category: skip-prediction
  artifacthub.io/license: MIT
  artifacthub.io/operator: "false"
  artifacthub.io/prerelease: "false"
  artifacthub.io/links: |
    - name: source
      url: https://github.com/quenchworks/charts
    - name: upstream
      url: https://github.com/TryGhost/Ghost
  artifacthub.io/signKey: |
    fingerprint: keyless (cosign / Sigstore)
    url: https://github.com/quenchworks/.github/blob/main/SECURITY.md
  artifacthub.io/images: |
    - name: ghost
      image: ghcr.io/quenchworks/images/ghost@sha256:f96aead1ba1374601dd8e235fe5b10580cff30771a88f8f927a598b398cefb3d
    - name: mariadb
      image: ghcr.io/quenchworks/images/mariadb@sha256:abf1c0823dcc7f2c1b77252defaa0202a386907d131e3ea62feb0bb07304a0f5
dependencies:
  - name: quench-common
    version: 0.0.2
    repository: oci://ghcr.io/quenchworks/charts
  - name: mariadb
    alias: mariadb
    version: 0.0.10
    repository: oci://ghcr.io/quenchworks/charts
    condition: mariadb.enabled
