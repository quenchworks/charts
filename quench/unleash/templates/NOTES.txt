Quenchworks Unleash {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} ({{ ternary "bundled" "external" .Values.postgresql.enabled }} PostgreSQL).

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 4242:{{ .Values.service.port }}
  open http://127.0.0.1:4242/

Admin user: {{ .Values.admin.username }}
{{- if and (not .Values.admin.password) (not .Values.admin.existingSecret) }}
Password:
  kubectl -n {{ .Release.Namespace }} get secret {{ include "quench-common.fullname" . }} -o jsonpath='{.data.admin-password}' | base64 -d
{{- end }}
The admin is created only on the first start against an empty database; change the
password in the UI afterwards.
