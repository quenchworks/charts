# Standalone (default topology) lint/template gate. No image override -- uses the
# chart's real, CI-maintained digest from values.yaml.
auth:
  enabled: true
  database: qwapp
primary:
  persistence:
    enabled: false

# Gate the metrics sidecar too: 0-CVE is not proof it starts and serves /metrics.
metrics:
  enabled: true
