Quenchworks Prefect {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}: the server{{ if .Values.worker.enabled }}, a process worker on pool "{{ .Values.worker.pool }}"{{ end }}
and {{ if .Values.postgresql.enabled }}a bundled PostgreSQL{{ else }}the external PostgreSQL at {{ .Values.externalDatabase.host }}{{ end }} for storage.

Open the UI (the browser calls {{ .Values.ui.apiUrl }}):

  kubectl port-forward svc/{{ include "quench-common.fullname" . }}-server {{ .Values.service.port }}:{{ .Values.service.port }}
  open http://127.0.0.1:{{ .Values.service.port }}

Point flows at the API:

  export PREFECT_API_URL=http://127.0.0.1:{{ .Values.service.port }}/api
