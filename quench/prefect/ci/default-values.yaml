# Install gate on kind: bundled PostgreSQL (small, no PVC) and the process worker, so
# the gate proves server <-> PostgreSQL <-> worker end to end.
worker:
  enabled: true
postgresql:
  primary:
    persistence:
      enabled: false
