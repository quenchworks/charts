# Install gate: a syntactically valid token for a GitLab that does not exist. The
# runner starts, serves metrics and retries the server; no job runs.
gitlabUrl: https://gitlab.invalid
runner:
  token: glrt-quench-ci-not-a-real-token
