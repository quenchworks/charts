# Install gate on kind: one replica and a local-data record, so the gate proves the
# chart's config snippet is loaded as well as recursion and DNSSEC.
replicas: 1
config: |
  server:
    local-data: "gate.quench.internal. A 10.1.2.3"
