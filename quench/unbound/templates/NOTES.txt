Quenchworks Unbound {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  DNS: {{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }} (UDP and TCP)

It answers clients in 127/8, 10/8, 172.16/12 and 192.168/16 and validates DNSSEC.
Try it from inside the cluster:

  kubectl run dnstest --rm -it --restart=Never --image=<an image with dig> -- \
    dig @{{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc example.com
