# Install gate on kind: the RocksDB store on a PVC, so the gate proves the native
# library loads on the read-only root and commits survive a pod restart.
versionStore:
  type: ROCKSDB
rocksdb:
  persistence:
    size: 1Gi
