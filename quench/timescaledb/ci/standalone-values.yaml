# Standalone install gate (the backward-compatible default topology). Also exercises
# the metrics sidecar. No image override: uses the chart's real, CI-maintained digest.
architecture: standalone
auth:
  enabled: true
  database: qwapp
primary:
  persistence:
    enabled: false
metrics:
  enabled: true
