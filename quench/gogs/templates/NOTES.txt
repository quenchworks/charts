Quenchworks Gogs {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} ({{ .Values.database.type }}).

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 3000:{{ .Values.service.port }}
  open http://127.0.0.1:3000/   (links point at {{ .Values.externalURL }})

Admin user: {{ .Values.admin.username }}
{{- if and (not .Values.admin.password) (not .Values.admin.existingSecret) }}
Generated password:
  kubectl -n {{ .Release.Namespace }} get secret {{ include "quench-common.fullname" . }} -o jsonpath='{.data.admin-password}' | base64 -d
{{- end }}
{{- if .Values.ssh.enabled }}

SSH clones use git@{{ .Values.sshDomain }}:{{ .Values.ssh.port }}; expose the ssh port of the Service to reach it.
{{- end }}
