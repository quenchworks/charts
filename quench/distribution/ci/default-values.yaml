# Standalone install gate. No image override: uses the chart's real CI-maintained digest.
persistence:
  enabled: false
resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits: { cpu: "1", memory: 512Mi }
