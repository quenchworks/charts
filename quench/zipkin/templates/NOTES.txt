Quenchworks Zipkin {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} with {{ .Values.storage.type }} storage.

  spans: http://{{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/api/v2/spans
  UI:    kubectl port-forward svc/{{ include "quench-common.fullname" . }} 9411:{{ .Values.service.port }}
         open http://127.0.0.1:9411/zipkin/
{{- if eq .Values.storage.type "mem" }}

In-memory storage keeps spans in this pod only and loses them on restart. Set
storage.type to elasticsearch, cassandra3 or mysql for anything you want to keep.
{{- end }}
