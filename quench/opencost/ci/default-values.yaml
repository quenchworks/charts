# Standalone install gate. No image override: the install uses the chart's real,
# CI-maintained digest from values.yaml. No Prometheus: the gate proves OpenCost comes
# up on its built-in collector data source and answers its API.
resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits: { cpu: 1000m, memory: 1Gi }
