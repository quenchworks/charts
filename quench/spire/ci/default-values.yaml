# Install gate on kind: the server keeps its datastore on a PVC, so the gate can check
# a registration entry survives a server restart.
trustDomain: gate.quench-works.com
clusterName: kind
server:
  persistence:
    enabled: true
    size: 1Gi
