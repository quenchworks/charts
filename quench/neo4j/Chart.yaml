apiVersion: v2
name: neo4j
# Neo4j Community Edition only (GPLv3). Enterprise (causal cluster, RBAC, hot backups)
# is commercial and NOT included; the GDS/GenAI and APOC plugins are dropped from the
# image to keep it GPLv3-clean. Single node: clustering is Enterprise-only (follow-up).
description: Quenchworks hardened Neo4j Community Edition (GPLv3), single node. Minimal, nonroot, 0-CVE image pinned by digest.
type: application
version: 0.0.1
appVersion: "2026.05.0"
icon: https://raw.githubusercontent.com/quenchworks/.github/main/profile/assets/chart-icon.png
home: https://quenchworks.mkabumattar.com
sources:
  - https://github.com/quenchworks/charts
  - https://github.com/neo4j/neo4j
annotations:
  artifacthub.io/license: GPL-3.0-only
  artifacthub.io/links: |
    - name: source
      url: https://github.com/quenchworks/charts
    - name: image
      url: https://github.com/quenchworks/images
  artifacthub.io/signKey: |
    fingerprint: keyless (cosign / Sigstore)
    url: https://github.com/quenchworks/.github/blob/main/SECURITY.md
dependencies:
  - name: quench-common
    version: 0.0.1
    repository: oci://ghcr.io/quenchworks/charts
