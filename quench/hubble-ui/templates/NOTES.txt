Quenchworks Hubble UI {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}, reading Hubble Relay at {{ .Values.relay.address }}.

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 12000:{{ .Values.service.port }}
  open http://127.0.0.1:12000/

The UI has no login: anyone who reaches the Service sees every flow in the cluster.
Keep the Service internal, or put an authenticating proxy in front of it.
