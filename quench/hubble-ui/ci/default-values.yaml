# Install gate on kind: the gate installs upstream Cilium and the QuenchWorks hubble-relay
# chart (release "hubble-relay" in kube-system) first; this UI reads that relay.
relay:
  address: hubble-relay.kube-system.svc.cluster.local:80
