# Install gate on kind: bundled PostgreSQL (small, no PVC) and the example code location,
# so the gate proves webserver <-> code server <-> PostgreSQL <-> daemon end to end.
example:
  enabled: true
postgresql:
  primary:
    persistence:
      enabled: false
