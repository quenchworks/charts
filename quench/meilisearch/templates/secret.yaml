{{/*
The chart-managed Secret carries the Meilisearch master key. It is rendered UNLESS the
user points auth.existingSecret at their own Secret. Key resolution:
  - auth.masterKey set            -> use that literal value
  - unset, prior Secret exists    -> preserve the previously generated key (lookup)
  - unset, no prior Secret        -> generate a strong 40-char random key once
This makes `helm upgrade` idempotent: the generated key survives upgrades, and
MEILI_ENV=production always has a key to satisfy.
*/}}
{{- if not .Values.auth.existingSecret }}
{{- $name := include "meilisearch.secretName" . -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $name -}}
{{- $key := .Values.auth.masterKey -}}
{{- if and (not $key) $existing -}}
{{- $key = index $existing.data "master-key" | b64dec -}}
{{- end -}}
{{- if not $key -}}
{{- $key = randAlphaNum 40 -}}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
type: Opaque
data:
  master-key: {{ $key | b64enc | quote }}
{{- end }}
