Quenchworks Easegress {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  admin API: http://{{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.adminPort }}/apis/v2 (unauthenticated, keep it internal)
{{- range .Values.trafficPorts }}
  traffic:   {{ include "quench-common.fullname" $ }}.{{ $.Release.Namespace }}.svc:{{ .port }} ({{ .name }})
{{- end }}

Manage objects with egctl from inside the pod:

  kubectl exec {{ include "quench-common.fullname" . }}-0 -- egctl get all
  kubectl exec -i {{ include "quench-common.fullname" . }}-0 -- egctl apply -f - < objects.yaml
