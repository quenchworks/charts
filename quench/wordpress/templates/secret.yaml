{{/*
The managed Secret always carries WordPress's 8 authentication keys/salts (auth-key,
secure-auth-salt, …) and — unless an external existingSecret supplies it — the DB password
under db-password. All are injected into the pod as WORDPRESS_* env and read by
wp-config.php, so no secret ever appears in the pod's argv / process list.

Salts and the DB password are RESOLVED from the existing Secret first (via lookup) so they
stay STABLE across upgrades — regenerating salts would invalidate every session/cookie, and
regenerating the DB password would break the connection to an already-initialized database.

When the bundled MySQL is enabled the DB password must be IDENTICAL to the one the MySQL
subchart provisions for its application user. Resolution order: explicit mysql.auth.password
-> the subchart's already-rendered Secret (<release>-mysql / mysql-password) via lookup ->
this Secret's prior value -> a fresh 24-char random. Set mysql.auth.password for a
deterministic install.
*/}}
{{- $name := include "quench-common.fullname" . -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $name -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
type: Opaque
data:
  {{- /* ---- auth keys/salts (always managed, stable across upgrades) ---- */}}
  {{- range $n := splitList " " (include "wordpress.saltNames" .) }}
  {{- $key := $n | lower | replace "_" "-" }}
  {{- $val := "" }}
  {{- if $existing }}{{- $val = index $existing.data $key | default "" | b64dec }}{{- end }}
  {{- if not $val }}{{- $val = randAlphaNum 64 }}{{- end }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
  {{- /* ---- DB password (managed unless an external existingSecret supplies it) ---- */}}
  {{- if include "wordpress.manageDbPassword" . }}
  {{- $pw := "" }}
  {{- if .Values.mysql.enabled }}
  {{- $pw = .Values.mysql.auth.password }}
  {{- if not $pw }}
  {{- $mysqlSecret := lookup "v1" "Secret" .Release.Namespace (printf "%s-mysql" .Release.Name) }}
  {{- if $mysqlSecret }}{{- $pw = index $mysqlSecret.data "mysql-password" | default "" | b64dec }}{{- end }}
  {{- end }}
  {{- if and (not $pw) $existing }}{{- $pw = index $existing.data "db-password" | default "" | b64dec }}{{- end }}
  {{- if not $pw }}{{- $pw = randAlphaNum 24 }}{{- end }}
  {{- else }}
  {{- $pw = .Values.externalDatabase.password }}
  {{- if and (not $pw) $existing }}{{- $pw = index $existing.data "db-password" | default "" | b64dec }}{{- end }}
  {{- if not $pw }}{{- $pw = randAlphaNum 24 }}{{- end }}
  {{- end }}
  db-password: {{ $pw | b64enc | quote }}
  {{- end }}
