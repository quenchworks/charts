# Install gate on kind: auth on (generated password) and KahaDB on a PVC, so the gate
# can check a wrong password is refused and messages survive a pod restart.
persistence:
  enabled: true
  size: 1Gi
storeLimit: 512 mb
tempLimit: 128 mb
