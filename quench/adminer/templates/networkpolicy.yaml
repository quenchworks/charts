{{/* Only the UI port. allowExternal defaults to false: Adminer will attempt a login
     against any database host the pod can reach, so same-namespace-only is the safe
     default and opening it is an explicit decision. */}}
{{- include "quench-common.networkPolicy" (dict "ctx" . "defaultPorts" (list (dict "port" "http" "protocol" "TCP"))) }}
