apiVersion: v2
name: authentik
description: Hardened Authentik identity provider (OIDC/SAML/LDAP/SCIM) on a 0-CVE image
type: application
version: 0.1.16
appVersion: "2026.5.6"
home: https://github.com/quenchworks
icon: https://raw.githubusercontent.com/quenchworks/.github/main/profile/assets/chart-icon.png
sources:
  - https://github.com/quenchworks/charts
  - https://github.com/quenchworks/images
  - https://github.com/goauthentik/authentik
keywords:
  - authentik
  - identity-provider
  - sso
  - oidc
  - saml
  - ldap
  - scim
  - authentication
  - security
maintainers:
  - name: mkabumattar
    url: https://github.com/quenchworks
annotations:
  artifacthub.io/changes: |
    - kind: security
      description: "anyio 4.14.2 (CVE-2026-63374 CRITICAL: TLSStream encoded host names with IDNA 2003, so a lookalike name could pass cert validation)"
  artifacthub.io/category: security
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Chart Source
      url: https://github.com/quenchworks/charts
    - name: Image Source
      url: https://github.com/quenchworks/images
    - name: Security Policy
      url: https://github.com/quenchworks/.github/blob/main/SECURITY.md
  artifacthub.io/images: |
    - name: authentik
      image: ghcr.io/quenchworks/images/authentik@sha256:33c2f7efc4a37e4166f256af23a55c3d1116689bdb90da6b61e8748a0f3ca537
      whitelisted: false
    - name: postgresql
      image: ghcr.io/quenchworks/images/postgresql@sha256:601897da3768720a98ee45b80303da7d4be2eb5b48a64c214dd4b290e04edc9d
    - name: valkey
      image: ghcr.io/quenchworks/images/valkey@sha256:ba49b17819b0b8c049b515c38471592465e152c183dde595269523ea35dc608d
    - name: redis
      image: ghcr.io/quenchworks/images/redis@sha256:3de1fede04c357e4d1f9f5e09b8cc5fef28ebc4b7e96e71d57ff07af5c6e8348
# Authentik has TWO HARD runtime dependencies: PostgreSQL and Redis. It refuses to
# boot without both. quench-common is the only always-on chart dependency; the DB and
# session store are each choosable as BUNDLED (the QuenchWorks subchart) or EXTERNAL
# (point at your own), mirroring the quench/authelia layout:
#   DATABASE modes:  bundled postgresql | external (externalDatabase.*)
#   REDIS modes:     bundled valkey | bundled redis | external (externalRedis.*)
#                    valkey is the recommended 0-CVE BSD store; redis is the AGPL
#                    alternative. Enable AT MOST ONE of valkey/redis.
# Unlike authelia (which can run standalone on SQLite), Authentik requires that
# EXACTLY ONE database mode and EXACTLY ONE redis mode resolve — the templates fail
# rendering with a clear message if a required backend is missing.
dependencies:
  - name: quench-common
    version: 0.0.8
    repository: oci://ghcr.io/quenchworks/charts
  - name: postgresql
    alias: postgresql
    version: 0.0.20
    repository: oci://ghcr.io/quenchworks/charts
    condition: postgresql.enabled
  - name: valkey
    alias: valkey
    version: 0.0.17
    repository: oci://ghcr.io/quenchworks/charts
    condition: valkey.enabled
  - name: redis
    alias: redis
    version: 0.0.18
    repository: oci://ghcr.io/quenchworks/charts
    condition: redis.enabled
