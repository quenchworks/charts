Quenchworks Cilium Operator {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}, reading ConfigMap {{ .Values.ciliumConfigMap }}.

  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get ciliumnodes

Run the Cilium agents with operator.enabled=false, so this is the only operator. The
chart's version should match the agents' Cilium minor line.
