# ArtifactHub repository metadata. For OCI repos this is pushed to the chart's
# OCI path with the reserved tag `artifacthub.io`, which lets ArtifactHub verify
# ownership and award the Verified Publisher badge. One ID per ArtifactHub repo.
# AH identifier convention: quench-apisix-ingress-controller,
# OCI URL oci://ghcr.io/quenchworks/charts/apisix-ingress-controller.
repositoryID: cff39444-ac35-4cc9-a137-80f4dfd43a3c   # apisix-ingress-controller (oci://ghcr.io/quenchworks/charts/apisix-ingress-controller)
owners:
  - name: mkabumattar
    email: info@quench-works.com
