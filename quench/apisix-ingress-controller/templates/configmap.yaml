apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "apisix-ingress-controller.configMapName" . }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- include "apisix-ingress-controller.configYaml" . | nindent 4 }}
