Quenchworks OPA Gatekeeper {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.
The webhooks skip: {{ join ", " (prepend .Values.exemptNamespaces .Release.Namespace) }} (and any namespace labelled admission.gatekeeper.sh/ignore).

  kubectl get constrainttemplates
  kubectl get constraints

Webhook failurePolicy is {{ .Values.validatingWebhook.failurePolicy }}: {{ if eq .Values.validatingWebhook.failurePolicy "Ignore" }}if Gatekeeper is down, requests pass unchecked.{{ else }}if Gatekeeper is down, matching requests are refused.{{ end }}
