# Install gate on kind: the bundled PostgreSQL on a small PVC; the API server's
# first start runs the schema migrations.
postgresql:
  primary:
    persistence:
      size: 1Gi
apiServer:
  resources:
    requests: { cpu: 250m, memory: 1Gi }
    limits: { cpu: "2", memory: 3Gi }
