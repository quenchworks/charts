Quenchworks OWASP Dependency-Track {{ .Chart.AppVersion }} is installed.

  UI and API: http://{{ include "dtrack.frontend" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/  (the UI proxies /api)
  API only:   http://{{ include "dtrack.api" . }}.{{ .Release.Namespace }}.svc:8080/api

  kubectl port-forward svc/{{ include "dtrack.frontend" . }} 8080:{{ .Values.service.port }}
  open http://127.0.0.1:8080  (first login admin / admin; you must change the password)

The first start runs database migrations; the API pod takes a few minutes to go Ready.
