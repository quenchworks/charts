Quenchworks Argo Rollouts {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} ({{ ternary "this namespace" "cluster-wide" .Values.namespaced }}).

  kubectl get rollouts -A
  kubectl argo rollouts get rollout <name>     # with the kubectl plugin

Helm installs the CRDs once. When the app version moves, apply the chart's crds/ with
kubectl apply --server-side before upgrading.
