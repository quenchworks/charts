# Standalone lint/template gate. The release workflow's kind gate deploys a real
# postgres-documentdb backend and overrides backend.url to point at it; this value
# just lets `helm template`/`helm lint` render (the chart requires a backend URL).
backend:
  url: "postgres://postgres:gate@placeholder:5432/postgres"
