# ArtifactHub repository metadata. Pushed to the chart's OCI path with the reserved tag
# `artifacthub.io` so ArtifactHub can verify ownership (Verified Publisher badge).
#
# ID taken from `ah.py list` (the org repositories listing), NOT `ah.py id` -- the search
# endpoint that backed `id` returned a wrong value for another chart. See ah/ah.py.
repositoryID: 0aa18123-5c33-4c2b-b076-e4f9aa991442   # matomo
owners:
  - name: mkabumattar
    email: info@quench-works.com
