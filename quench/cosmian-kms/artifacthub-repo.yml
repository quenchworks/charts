# ArtifactHub repository metadata. For OCI repos this is pushed to the chart's
# OCI path with the reserved tag `artifacthub.io`, which lets ArtifactHub verify
# ownership and award the Verified Publisher badge. One ID per ArtifactHub repo.
# AH identifier: quench-cosmian-kms, OCI oci://ghcr.io/quenchworks/charts/cosmian-kms.
#
# ID taken from `ah.py list` (org repositories listing), not `ah.py id` -- the latter
# uses AH's search endpoint and disagreed with the listing for another chart.
repositoryID: b24e184f-e1a2-4418-930e-e94e6900fc04   # cosmian-kms
owners:
  - name: mkabumattar
    email: info@quench-works.com
