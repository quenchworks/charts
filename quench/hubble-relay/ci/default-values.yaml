# Install gate on kind: upstream Cilium runs as the CNI with Hubble on and Hubble TLS off,
# and its own relay off. This relay dials the agents' peer service in plaintext.
peerService: hubble-peer.kube-system.svc.cluster.local:80
tls:
  client:
    enabled: false
