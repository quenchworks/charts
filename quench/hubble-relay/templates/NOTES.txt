Quenchworks Hubble Relay {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}, dialing {{ .Values.peerService }}.

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 4245:{{ .Values.service.port }}
  hubble status --server localhost:4245
  hubble observe --server localhost:4245 --last 20

The pod turns ready once at least one Hubble peer is connected. If it stays unready,
check that Cilium runs with Hubble enabled and that peerService and the TLS settings
match it (Cilium's Hubble TLS is on by default: port 443 plus the client Secret).
