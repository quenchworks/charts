# Install gate on a single-node kind cluster. kind labels its node
# node.kubernetes.io/exclude-from-external-load-balancers, so the speaker must ignore
# that label to announce anything there. Images come from values.yaml (real digests).
speaker:
  ignoreExcludeLB: true
