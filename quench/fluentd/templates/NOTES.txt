Quenchworks Fluentd {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} ({{ .Values.replicas }} replica(s)).
{{ range .Values.ports }}
  {{ .name }}: {{ include "quench-common.fullname" $ }}.{{ $.Release.Namespace }}.svc:{{ .port }}
{{- end }}

Point collectors (Fluent Bit, Vector, fluent-logger libraries) at the forward port. The
pipeline is the `config` value; buffers under /fluentd/buffer live on the PVC.
