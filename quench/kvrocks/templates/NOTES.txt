Quenchworks Apache Kvrocks {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  address:  {{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }} (Redis protocol)
  password: kubectl get secret {{ include "kvrocks.secretName" . }} -o jsonpath='{.data.password}' | base64 -d

Any Redis client works, e.g. with the QuenchWorks valkey image:

  kubectl run kvcli --rm -it --restart=Never --image=ghcr.io/quenchworks/images/valkey:9.1.2 \
    --command -- valkey-cli -h {{ include "quench-common.fullname" . }} -p {{ .Values.service.port }} -a "$PASSWORD" PING
