# Default install = destination only: boots with zero configuration and no
# certificate material. The release gate separately proves the identity and
# policy-controller paths by generating real cert material and flipping them on
# (see .github/workflows/release-linkerd.yml).
