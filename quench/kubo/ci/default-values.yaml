# Install gate on kind: persistence on, so the gate proves the repo (blocks and the
# node identity) survives a pod restart on the PVC.
persistence:
  enabled: true
  size: 1Gi
resources:
  requests: { cpu: 100m, memory: 256Mi }
  limits: { cpu: 1000m, memory: 1Gi }
