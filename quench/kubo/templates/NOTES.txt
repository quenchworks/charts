Quenchworks Kubo (IPFS) {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  gateway:  http://{{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.gatewayPort }}/ipfs/<cid>
  RPC API:  http://{{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.apiPort }}/api/v0 (unauthenticated, keep it internal)

Add a file and read it back:

  kubectl port-forward svc/{{ include "quench-common.fullname" . }} 5001 8080 &
  curl -X POST -F file=@README.md "http://127.0.0.1:5001/api/v0/add?cid-version=1"
  curl http://127.0.0.1:8080/ipfs/<cid>
