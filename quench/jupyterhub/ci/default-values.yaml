# Install gate: a fixed password, no persistence.
auth:
  password: quench-ci-password
  adminPassword: quench-ci-admin-password-32-characters-long
hub:
  persistence:
    enabled: false
singleuser:
  storage:
    enabled: false
