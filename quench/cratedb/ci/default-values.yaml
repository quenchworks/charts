# Install gate on kind: one node with persistence, so the gate can check data survives
# a pod restart.
persistence:
  enabled: true
  size: 1Gi
resources:
  requests: { cpu: 250m, memory: 1Gi }
  limits: { cpu: "2", memory: 2Gi }
