# ArtifactHub repository metadata, pushed to the chart's OCI path with the reserved tag
# `artifacthub.io` so ArtifactHub can verify ownership.
#
# ID taken from `ah.py list` (org repositories listing), NOT `ah.py id`'s old search path --
# see ah/ah.py for why that returned a wrong value for another chart.
repositoryID: d329198e-81ac-479f-8326-7bd95c6c390a   # k6 (oci://ghcr.io/quenchworks/charts/k6)
owners:
  - name: mkabumattar
    email: info@quench-works.com
