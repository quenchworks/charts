# Boots with zero configuration: pilot-discovery authenticates in-cluster with
# its own ServiceAccount and comes ready once discovery is up. This file exists
# so the release gate renders exactly what users get by default.
