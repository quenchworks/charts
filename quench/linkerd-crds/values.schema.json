{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "quench/linkerd-crds",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "crds": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "keep": {
          "type": "boolean",
          "description": "Stamp helm.sh/resource-policy: keep on every CRD so helm uninstall cannot cascade-delete every Server, ServerAuthorization and ServiceProfile in the cluster."
        }
      }
    },
    "global": { "type": "object" }
  }
}
