{{- if not .Values.config.existingConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "coredns.configMapName" . }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
data:
  # Mounted at /etc/coredns/Corefile (the image's -conf path).
  Corefile: |-
    {{- .Values.config.corefile | nindent 4 }}
{{- end }}
