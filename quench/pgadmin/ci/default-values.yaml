# Install gate on kind: the admin from values, a config key from values, a small PVC.
admin:
  email: gate@example.com
  password: Quench-gate-Pw-1
config:
  MAX_LOGIN_ATTEMPTS: 5
persistence:
  size: 1Gi
