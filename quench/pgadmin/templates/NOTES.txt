Quenchworks pgAdmin {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 5050:{{ .Values.service.port }}
  open http://127.0.0.1:5050/   and sign in as {{ .Values.admin.email }}
{{- if and (not .Values.admin.password) (not .Values.admin.existingSecret) }}

  kubectl -n {{ .Release.Namespace }} get secret {{ include "quench-common.fullname" . }} -o jsonpath='{.data.admin-password}' | base64 -d
{{- end }}

The admin is created only on the first start with an empty data volume.
