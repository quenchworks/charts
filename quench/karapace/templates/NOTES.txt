Quenchworks Karapace {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}, connected to {{ .Values.kafka.bootstrapServers }}.

  schema registry: http://{{ include "quench-common.fullname" . }}-registry.{{ .Release.Namespace }}.svc:{{ .Values.service.registryPort }}
{{- if .Values.restProxy.enabled }}
  REST proxy:      http://{{ include "quench-common.fullname" . }}-rest-proxy.{{ .Release.Namespace }}.svc:{{ .Values.service.restProxyPort }}
{{- end }}

Both speak the Confluent APIs, so Confluent serializers and clients work unchanged.
Neither has authentication by default; keep the Services internal or configure
Karapace's registry_authfile / rest_authorization through config.
