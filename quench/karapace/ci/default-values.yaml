# Install gate on kind: the workflow installs the QuenchWorks kafka chart as release
# "kq" (one broker) first; the registry and the REST proxy connect to it.
kafka:
  bootstrapServers: "kq-kafka:9092"
restProxy:
  enabled: true
