{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "emqx.secretName" . }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
type: Opaque
data:
  dashboard-password: {{ .Values.dashboardPassword | b64enc | quote }}
{{- end }}
