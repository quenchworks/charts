Quenchworks MariaDB is installed as {{ include "quench-common.fullname" . }}.

Connect from inside the cluster:

  Host: {{ include "quench-common.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  Port: {{ .Values.service.port }}
{{- if .Values.auth.database }}
  Database: {{ .Values.auth.database }}
{{- end }}

Get the root password:

  kubectl get secret --namespace {{ .Release.Namespace }} {{ include "mariadb.secretName" . }} \
    -o jsonpath="{.data.{{ include "mariadb.rootPasswordKey" . }}}" | base64 -d

Then, from a client pod:

  mariadb -h {{ include "quench-common.fullname" . }} -uroot -p"<root-password>"

The image is pinned by digest and signed. Verify it:

  cosign verify {{ .Values.image.repository }} \
    --certificate-identity-regexp 'https://github.com/quenchworks/.+' \
    --certificate-oidc-issuer https://token.actions.githubusercontent.com
