apiVersion: v2
name: identity-stack
description: "Hardened, operator-free self-hosted SSO/identity stack: Keycloak (OIDC/SAML identity provider) + PostgreSQL (Keycloak's database) + oauth2-proxy (an auth proxy you place in front of any upstream app to authenticate users against Keycloak), wired together so you get single sign-on in front of your apps out of the box. No operator, no CRDs. All components are QuenchWorks-hardened, nonroot, 0-CVE images pinned by digest and cosign-signed."
type: application
version: 0.0.12
appVersion: "1.0.0"
keywords:
  - identity
  - sso
  - oidc
  - keycloak
  - oauth2
  - oauth2-proxy
  - postgresql
  - iam
  - authentication
  - stack
maintainers:
  - name: QuenchWorks
    email: info@quench-works.com
    url: https://github.com/quenchworks
icon: https://raw.githubusercontent.com/quenchworks/.github/main/profile/assets/chart-icon.png
home: https://quench-works.com
sources:
  - https://github.com/quenchworks/charts
  - https://github.com/keycloak/keycloak
  - https://github.com/oauth2-proxy/oauth2-proxy
  - https://github.com/postgres/postgres
annotations:
  artifacthub.io/changes: |
    - kind: security
      description: "Corrected the advertised image digests to what the bundled subcharts actually deploy: Keycloak 26.7.2 (fixes CVE-2026-18963, CRITICAL) and the rebuilt PostgreSQL 18.6."
  artifacthub.io/category: security
  artifacthub.io/images: |
    - name: keycloak
      image: ghcr.io/quenchworks/images/keycloak@sha256:7e9bd0bbbb317589e0fabde9cbddafcd22fdfd79716a3feb621efcbf40fe9184
    - name: postgresql
      image: ghcr.io/quenchworks/images/postgresql@sha256:919b69c5c86b3a2a454bbe50a24d9d33feb73441bb0d466e583bc3e1a78ac77b
    - name: oauth2-proxy
      image: ghcr.io/quenchworks/images/oauth2-proxy@sha256:6c479b032dcd1751b53a88d8b8eb66b1a2d7263a405b74dcecfc9709800b5a13
  artifacthub.io/operator: "false"
  artifacthub.io/prerelease: "false"
  artifacthub.io/license: Apache-2.0
  artifacthub.io/links: |
    - name: source
      url: https://github.com/quenchworks/charts
    - name: image
      url: https://github.com/quenchworks/images
  artifacthub.io/signKey: |
    fingerprint: keyless (cosign / Sigstore)
    url: https://github.com/quenchworks/.github/blob/main/SECURITY.md
dependencies:
  - name: quench-common
    version: 0.0.8
    repository: oci://ghcr.io/quenchworks/charts
  # PostgreSQL is NOT a direct dependency: the keycloak chart already bundles
  # quench/postgresql (alias "postgresql", default enabled) and wires Keycloak to
  # it automatically (keycloak.db.host resolves to <release>-postgresql). It still
  # ships in this stack (inside keycloak), so it stays in artifacthub.io/images
  # and the keywords above.
  - name: keycloak
    version: 0.0.22
    repository: oci://ghcr.io/quenchworks/charts
    condition: keycloak.enabled
  - name: oauth2-proxy
    alias: oauth2Proxy
    version: 0.0.9
    repository: oci://ghcr.io/quenchworks/charts
    condition: oauth2Proxy.enabled
