Quenchworks AKHQ {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }}.

  kubectl port-forward svc/{{ include "quench-common.fullname" . }} 8080:{{ .Values.service.port }}
  open http://127.0.0.1:8080/ui
{{- if and (not .Values.connections) (not .Values.existingSecret) }}

No Kafka cluster is configured, so AKHQ reports unhealthy and the pod stays unready.
Set connections (see values.yaml) and upgrade.
{{- end }}

AKHQ has no login by default: anyone who reaches the UI can read and write topics.
Keep the Service internal, or configure akhqConfig.security.
