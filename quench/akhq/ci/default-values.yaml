# Install gate on kind: the workflow installs the QuenchWorks kafka chart as release
# "kq" first, and AKHQ connects to it.
connections:
  gate:
    properties:
      bootstrap.servers: "kq-kafka:9092"
