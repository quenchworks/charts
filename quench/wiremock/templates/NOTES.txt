Quenchworks WireMock {{ .Chart.AppVersion }} is installed as {{ include "quench-common.fullname" . }} with {{ len .Values.mappings }} stub file(s) from values.

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quench-common.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://127.0.0.1:8080/__admin/mappings

The admin API (/__admin) is open to anyone who reaches the Service and can add, change
and delete stubs. Keep the Service internal to the test environment.
