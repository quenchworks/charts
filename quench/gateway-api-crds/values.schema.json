{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Quenchworks gateway-api-crds values",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "crds": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "keep": {
          "description": "Stamp helm.sh/resource-policy: keep on every CRD so helm uninstall cannot cascade-delete every Gateway/HTTPRoute in the cluster.",
          "type": "boolean"
        }
      }
    },
    "safeUpgradePolicy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Ship upstream's safe-upgrades ValidatingAdmissionPolicy. Self-skips below Kubernetes 1.30.",
          "type": "boolean"
        }
      }
    },
    "global": {
      "description": "Helm reserves .Values.global for parent charts; accepted and ignored.",
      "type": "object"
    }
  }
}
