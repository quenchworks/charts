{{- if eq .Values.architecture "replication" }}
# Read-scaling Service (all replicas). Writes must go to the primary Service or,
# under Sentinel, to whichever node SENTINEL get-master-addr-by-name reports.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dragonfly.replica.fullname" . }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
    app.kubernetes.io/component: replica
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      name: redis
  selector:
    {{- include "quench-common.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: replica
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-headless" (include "dragonfly.replica.fullname" .) }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
    app.kubernetes.io/component: replica
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      name: redis
  selector:
    {{- include "quench-common.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: replica
{{- end }}
