# Install gate. Small PVCs and resources for one kind node.
keycloak:
  resources:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: "2", memory: 1Gi }
  postgresql:
    primary:
      persistence:
        size: 1Gi
openbao:
  persistence:
    size: 1Gi
