{{- if not .Values.existingConfigmap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" (include "quench-common.fullname" .) }}
  labels:
    {{- include "quench-common.labels" . | nindent 4 }}
data:
  redis.conf: |
{{ .Values.config | indent 4 }}
{{- end }}
