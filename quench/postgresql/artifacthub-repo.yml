# ArtifactHub repository metadata. For OCI repos this is pushed to the chart's
# OCI path with the reserved tag `artifacthub.io`, which lets ArtifactHub verify
# ownership and award the Verified Publisher badge. One ID per ArtifactHub repo.
#
# TODO(quenchworks): create the ArtifactHub repository for this chart
#   (oci://ghcr.io/quenchworks/charts/postgresql, suggested name `quench-postgresql`)
#   and paste its repositoryID below. Until then the Verified Publisher badge will
#   not link for postgresql; the schema and signing still work.
repositoryID: REPLACE_WITH_QUENCH_POSTGRESQL_REPOSITORY_ID   # quench-postgresql (oci://ghcr.io/quenchworks/charts/postgresql)
owners:
  - name: mkabumattar
    email: quenchworks@mkabumattar.com
