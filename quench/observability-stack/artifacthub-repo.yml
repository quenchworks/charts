# ArtifactHub repository metadata. For OCI repos this is pushed to the chart's
# OCI path with the reserved tag `artifacthub.io`, which lets ArtifactHub verify
# ownership and award the Verified Publisher badge. One ID per ArtifactHub repo.
# AH identifier convention: quench-observability-stack,
# OCI URL oci://ghcr.io/quenchworks/charts/observability-stack.
repositoryID: b35aa9cc-1558-4053-b967-ae9f74250035   # observability-stack (oci://ghcr.io/quenchworks/charts/observability-stack)
owners:
  - name: mkabumattar
    email: info@quench-works.com
